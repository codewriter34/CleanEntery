{
  "name": "clean-entry",
  "version": "0.1.0",
  "description": "Lightweight Express middleware and utility to scrub and guard incoming data against rubbish, spam, SQL injection, and XSS.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"no linter configured\"",
    "test": "tsx --test test/cleanEntry.test.ts",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "express",
    "middleware",
    "security",
    "sql-injection",
    "xss",
    "validation",
    "sanitization"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "dependencies": {
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/validator": "^13.15.10",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
